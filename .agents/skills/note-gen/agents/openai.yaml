interface:
  display_name: "Note Gen"
  short_description: "Repo-specific patterns and workflows for note-gen"
  default_prompt: "Use the note-gen repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true